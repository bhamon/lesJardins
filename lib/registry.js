'use strict';

/**
	@module		registry
	@desc		Application-wide object registry.
				This module is used both to register and retrieve static objects.
				The registry will likely be filled by init scripts during components initialization.
*/

/**
	@desc	Object collection.
*/
module.exports = {};